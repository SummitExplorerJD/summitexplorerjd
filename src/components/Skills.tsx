import { FC } from "react";

const Skills: FC = () => {
    return (
        <section id='Habilidades' className="h-screen">
            <h1>Habilidades</h1>
        </section>
    );
};

export default Skills;