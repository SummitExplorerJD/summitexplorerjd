import { FC } from "react";

const Projects: FC = () => {
    return (
        <section id='Proyectos' className="h-screen">
            <h1>Proyectos</h1>
        </section>
    );
}

export default Projects;