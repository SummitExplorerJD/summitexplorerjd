import { FC } from "react";

const Contact: FC = () => {
    return (
        <section id="Contacto" className="h-screen">
            <h1>Contacto</h1>
        </section>
    );
}

export default Contact;