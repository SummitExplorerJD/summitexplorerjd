import { FC } from "react";

const Services: FC = () => {
    return (
        <section id='Servicios' className="h-screen">
            <h1>Servicios</h1>
        </section>
    );
}

export default Services;